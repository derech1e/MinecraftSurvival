name: MinecraftSurvival
version: ${project.version}
main: de.thomas.MinecraftSurvival
api-version: 1.19
authors: [ Thomas ]
description: Ein Plugin für das Survival Projekt
load: POSTWORLD
commands:
  ping:
    description: Gibt den Ping zum Server an
  fly:
    description: Lass dich fliegen!
  chunkloader:
    description: Lasse chunks laden
